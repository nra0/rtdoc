#include "cli.h"

#include <stdio.h>


/*
 * Client main method.
 * Starts a client instance that interacts with a server at a given port.
 *
 * @param port: The port the server is running on.
 */
void clientStart(unsigned int port) {
  printf("Starting client... Listening to port %d\n", port);
}
